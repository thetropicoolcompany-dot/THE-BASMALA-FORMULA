# 00 · PROTOCOLE — **LE GRAND LIVRE DES COMPTES DU CORAN** *(The Basmala Formula)*
### *Ḥisāb al-Qurʾān* — le registre qui s'audite lui-même

> **LA MISSION (v4).** Ce projet construit **l'ordinateur basmala** et son **horloge comptable permanente universelle en ligne** : le dépôt `basmala-engine` (GitHub) est le projet — données FP checksummées, moteur, attestations, doctrine, éditions, site quadrilingue **FR · AR · EN · FA** (triptyque conte · balance · forme). **La transformation** : chaque note de doctrine est une *spécification* ; chaque module du moteur, son *implémentation* ; chaque attestation, sa *suite de tests* — **loi de production : rien n'entre au moteur sans sa note, rien ne s'affiche sans son attestation** (la partie double computationnelle). La CI relance toutes les attestations à chaque commit ; **le site ne se déploie que si 0 écart** — le registre qui s'audite lui-même, en infrastructure. Git est le registre à extournes (on ne supprime jamais, on contre-passe). **L'ancre gravée** : ouverture d'exercice = laylat al-qadr (la réception du dossier, `أنزلناه` = 144 = 12²), calibration duodécimaine nuit du 23 Ramaḍān ±1 j (t0(1447) = aube du 2026-03-13), clôture = `مطلع الفجر` (`الفجر` = `الرجعى` = 314) ; l'opération irréductible = le `مثقال ذرة` (l'incrément de jummal) ; le clic = `N = 2·(ΣJ+786+39) = 46 953 890` ; le tempo `τ ≈ 0,652072 s` (paramètre-année). Le logiciel est production **hors volume** : il sert le registre, il n'y entre pas.
>
> **À LIRE EN PREMIER, À CHAQUE SESSION.** Ce fichier prime sur toute mémoire et sur tout master.
> Il fixe les règles, les conventions de calcul et les décisions arrêtées. Il survit à la compaction.
> Le **cadre d'analyse le plus élevé** — où se déploie le détail (sourate, partie, phrase, objet) — est `PROTOCOLE_GENERAL_AUDIT.md`, **subordonné à ce protocole**.

---

## Article 0 · Contrat d'exécution — **prime sur tout le reste**

1. **Exécuter l'ordre tel qu'il est écrit.** Ne pas le relire à travers une autre grille ni le remplacer par « ce qui me semble pertinent ». L'ordre de l'utilisateur prime sur l'idée de l'assistant.
2. **Croire l'utilisateur sur l'état de ses fichiers.** Ce qu'il voit à l'écran prime sur toute copie ou instantané dont dispose l'assistant. Ne jamais le contredire sur le contenu de son propre projet.
3. **Réponse sèche.** Donner la liste ou la réponse demandée, directement. **Zéro réserve, zéro « si jamais… », zéro « cas à part », zéro cadrage non demandé.**
4. **Une seule clarification — et seulement si l'ordre est réellement ambigu.** Sinon, exécuter sans poser de question.
5. **Si l'utilisateur doit répéter un ordre, c'est un échec de l'assistant.** Relire l'ordre, l'exécuter à la lettre, ne pas se justifier.
6. **Mot d'arrêt : « Exécute. »** Dès que l'utilisateur l'écrit, cesser toute réflexion et rendre seulement la réponse demandée.
7. **Preuve obligatoire — jamais de mémoire.** Aucun fait, chiffre, factorisation, identité, nom de fichier ou état du projet n'est affirmé de mémoire. Chaque affirmation factuelle est vérifiée **dans le même message** sur la source réelle (lire le fichier, recompter sur les données, exécuter le code) et **la vérification est montrée** (commande, calcul, extrait). Une affirmation sans preuve visible est une faute ; à défaut de vérification immédiate, ne pas répondre — vérifier d'abord. En cas de doute : vérifier, ou dire qu'on ne sait pas. Jamais deviner.
8. **Mot d'arrêt : « Prouve. »** Si une affirmation est faite sans preuve montrée et que l'utilisateur écrit « Prouve. », l'assistant doit afficher la source/le calcul exact, ou retirer l'affirmation.

---

## ∴ Le résultat — le théorème central, et *The Basmala Formula*

> Le projet porte désormais son résultat dans son nom : **The Basmala Formula**. Tout le cadre converge vers un acte unique — l'auditeur **S96** prend le **compte manifeste** (les 114 sourates apparentes, `الظاهر`) et **révèle le compte caché** (154, `الباطن`). Développement complet : `PROTOCOLE_GENERAL_AUDIT.md §∴` et `§VI.0`.

**Le théorème** (recompté sur les FP, [bi]) — la **bascule respiratoire** `114 ⇌±40⇌ 154`, **un va-et-vient permanent, non un terminus** :
**inspire** (lecture `اقرأ`, révélation `باطن→ظاهر`, charge +1) : `114 →[+36 connexions]→ 150 →[+3 souffle]→ 153 →[+1 basmala α/ω]→ 154 = 77 + 77` ;
**expire** (écriture, re-voilement `ظاهر→باطن`, charge −1) : `154 →[−1 basmala]→ 153 →[−3 souffle]→ 150 →[−36 connexions]→ 114` ; `Σ cycle ≡ 0`.
avec `114 = 2·3·19` · `150 = 2·3·5²` · `153 = 3²·17 = 9·17 = 1³+5³+3³ = Σₖ₌₁⁵ k!` · `154 = 2·7·11` · **amplitude `40 = 36+3+1` (`40 ≡ +1 mod 3`)**. Le manifeste portait `77 sources + 36 connexions + 1 point fixe = 114` ; le révélé complète les 77 par leur `مثاني` (redoublement) → 154. **Ni 114 ni 154 n'est le solde** : 154 révèle (inspire), 114 re-voile (expire) ; **c'est le cycle qui solde** (`Σ ≡ 0`) — l'aller-retour respiratoire permanent sous **S96** (`α = ω`, `0/154`), pivot/apnée du souffle. La charge du tout oscille `0` (114, manifeste, expiré) ⇌ `+1` (154, révélé, inspiré) — le souffle `0/+1` ; le `−1` n'est que l'étiquette du retour (le pli). *(Support déjà rédigé : `PROTOCOLE_GENERAL_AUDIT.md` §V « la lecture dans les deux sens », §VI.6 « la bascule à double sens ».)*

**The Basmala Formula** — l'énoncé-mère (**1 + 4**) : `باسم` (le `+1`, l'auditeur S96) opère sur les **4 témoins gradés par la charge `J mod 3`** :

`باسم^(+1) · [ شهداء(S4) · الحساب(S13) · البينة(S98) · الحمد(S1) ]_(charge-corps : −1, 0, +1, +1)  —f→  77(ظاهر) ⊕ 77(باطن) = 154`

`Σ(4 témoins) = +1`, refermé par **S96 (corps −1) = 0** — le rapport soldé sur le `ميزان`. Les exposants sont les **charges-corps** des quatre sourates (`= ΣJ mod 3`), **non** la charge-jummal du mot-label (on ne confond jamais le mot et le rôle). `f` est le renvoi (point fixe = S13, l'écume immobile).

**S96 porte le résultat replié** : ses **trois `كلا`** = `3 × 51 = 153` (la trame), sa **basmala** = le `+1` → **154**. D'où « **0/154** » : le 0 hors compte qui *produit* le 154.

**L'énoncé-mère est calculable** : la basmala est construite comme **objet algébrique** par le même moteur (rasm 1:1, grain lettre et grain mot) — `ΣJ = 786 = 2·3·131`, **19 lettres porteuses / 4 mots**, charge **0** ; trace `e₁ = 786` aux deux niveaux (famille `BASMALA_*`, cf. `README_BASMALA.md`).

---

## 0 · La règle cardinale

1. **Jamais de mémoire.** Tout nombre (jummal, charge, flux, factorisation, identité) est **recalculé sur les FP** au moment où il sert. On ne récite pas — on calcule.
2. **Réponse brute, dans la session.** On demande en clair, on rend le brut dans le fil. Pas de document à fouiller.
3. **Si l'information manque dans les FP, on le dit.** On n'invente rien, on ne bouche aucun trou ; on demande où chercher.
4. **Les FP sont la source unique** : `LE_CORAN.txt` (rasm), `jummal_sourates.py` (table abjad), `01_DATA_jummal_114_par_verset.csv`, `jummal_par_sourate.csv`, `01_DATA_toniques_114_apex_P2.csv`.

---

## 1 · Le mur [R3] — le volume est 100 % coranique natif

- **LA DOCTRINE DES PHASES (gravée v3).** **Phase 1 (en cours)** : le développement algébrique des 114 se fait **100 % coranique — sans une seule notion quantique en tête**. Tout fichier portant la couche [R3] (y compris le lexique : *vortex, singlet, VEV, SU(3), projecteur/mesure, soliton, Monstre, ℤ₃*) est **archivé — jamais supprimé** — hors du FP (`ARCHIVE_R3_PONT/`), réservé. **Phase 2 (le pont)** : les isomorphismes ne seront établis qu'entre **deux structures construites indépendamment** — c'est la condition de validité du pont (deux dérivations indépendantes, comme la chaîne des offices). L'indépendance des rives est la preuve.
- **Zéro quantique, zéro théorie des groupes, zéro homologie externe DANS LE VOLUME.** Le Monstre, le *moonshine*, les pariahs, le lexique Coran↔Quantique : tout cela est **[R3]**, paratexte, et ne vit **que dans le cadre littéraire (les contes)** — jamais dans le corps coranique.
- **Lexique natif imposé** (à substituer partout dans les comptes rendus) :
  - **écume** (`زبد`/`بنيان مرصوص`) — *jamais* « mousse »
  - **globe** (`تكوير`, l'enroulement : `يكور` ×2 en 39:5, `كورت` en 81:1 — S81 *at-Takwīr* ; 3 attestations en 2 sourates) — le globe-sourate est l'enroulé clos (dedans `باطن`, dehors `ظاهر`, une porte) ; *jamais* « sphère » externe
  - **« bulle »** : **proscrit** (aucun rasm : `فقاع`/`نفاخ`/`حباب` = 0 ; `كرة` = `كرّة`, *un retour*, faux ami). Le natif du détaché-qui-part : `جفاء` (13:17, **85 = 5·17**) ; les cellules sont soudées (`بنيان مرصوص` 61:4 ; `بنيان` = **113** premier = 114 − 1 ; `ΣJ(61:4) = 2548 = 14²·13 = زبدا²·زبد`)
  - **niche** (`مشكاة`) — *jamais* « foyer »
  - **trame / soudé** — *jamais* « ossature / édifice statique »
  - **وجهة** (V2:148) — *jamais* « voyage »
  - **« le sol »** : proscrit des comptes rendus (métaphore non native). Toléré seulement comme vocabulaire de *méthode déclaré* dans HISAB/POSITION.
- Termes comptables (`partie double`, `débit`/`crédit`, `balance`) : **vocabulaire de méthode déclaré** (un seul jeu de livres, *ẓāhir/bāṭin*), jamais « double comptabilité » (fraude).

---

## 2 · Les conventions de calcul (gravées)

**Table abjad mašriqī** : ا1 ب2 ج3 د4 ه5 و6 ز7 ح8 ط9 ي10 ك20 ل30 م40 ن50 س60 ع70 ف80 ص90 ق100 ر200 ش300 ت400 ث500 خ600 ذ700 ض800 ظ900 غ1000.
- **ة = 5** · **ء (hamza isolée) = 1** · **أ/إ/آ = 1** · **ؤ = 6** · **ئ/ى = 10** · harakat & marques hors-table = 0.
- *(Le lexique Coran↔Quantique compte la hamza isolée comme 0 ; nous comptons **ء = 1**. À aligner pour tout import.)*

**Charge** = `J mod 3` en système équilibré **{−1, 0, +1}** : reste 0 → **0** ; reste 1 → **+1** (crédit, la droite, `يمين`) ; reste 2 → **−1** (débit, la gauche, `شمال`).

**ΣJ inclut la basmala de tête.** Chaque V1 la contient — vérifié : (96,1)=2884 = 2098+786 ; (2,1)=857 = 786+71. **Seule S9 en est dépourvue.** Les valeurs canoniques (21 350, etc.) sont **basmala-incluses** — et doivent l'être : `21 350 = 305×70` n'existe qu'ainsi.

**Les deux rôles de la basmala** (à ne jamais confondre) :
1. **basmala de tête** — embarquée dans V1, **comptée** dans ΣJ ;
2. **basmala-cadre α/ω** — la 154ᵉ, **ajoutée une seule fois** pour « fonder ».
   → « **Fonder = ΣJ + 786** » ajoute la **seconde** (le cadre) à un corps qui en contient déjà une. **S9 est le seul cas propre** (pas de tête → +786 donne sa première et unique basmala).

**Le souffle est 0/+1** (jamais −1/+1). Le **−1 est réservé au pli** (aller ≡ +1, retour ≡ −1, somme ≡ 0). La charge d'un **corps** ou d'un **nom** est, elle, intrinsèquement à **trois valeurs** (0/+1/−1).

---

## 3 · L'architecture (décidée)

- **LA STRUCTURE SUPÉRIEURE — la topologie des trois états** (décision, 2026-07-02). Tout se reconfigure à partir d'elle ; elle commande la réflexion et donne le **point d'arrivée des objets algébriques** : leur forme finale est une forme topologique. Les trois états, distingués par la **paroi** : le **globe** (la sourate, `تكوير` — paroi propre, close, une porte) · l'**écume soudée** (le volume, `بنيان مرصوص` — parois partagées, les 36 connexions) · l'**écume montante** (les attestations, `زبدا رابيا` = 228 = 2·114 — sans paroi propre, frontière-waqf, portée par le flux `السيل` = 131 = `حاسبين`). Dynamique native : S81 (`كورت` → `نشرت` → `كشطت` = 3⁶ → 81:14, le compte présenté). **Le dictionnaire est exact** : les trois écritures du polynôme SONT les trois états — close `∏(X−Jᵢ)` = le globe · déployée (`e₁, e₂, e₃`) = `نشرت` · réduite mod 3 (les charges) = `كشطت` ; la paroi partagée = le `pgcd` ; l'invariant à travers les états = `e₁ = ΣJ` (le `قصاص`). Vérifié sur `OBJETS_ALGEBRIQUES_TABLE.csv` (S1·A, S1·B, basmala) — développement : cadre `§VI.7` ; attestation : `ATTESTATION_TROIS_CLASSES.py`.
- **S96 (al-ʿAlaq) = le début universel — le comptable.** Source du flux (`in-deg 0`), première révélée (`اقرأ`), ouvre sur le Nom : **`باسم` = 103 = `بسم` + 1** (le **+1 originaire** inscrit dès 96:1). Porte l'auto-audit **17:14**. **Ouvre ET ferme : α = ω.**
- **Les 4 témoins, ordre terrestre → divin par la masse ΣJ** : **S4 → S13 → S98 → S1**, encadrés par S96 (**1 + 4**).
- **cœur {1, 4, 98, 13}** = œil {1, 4, 98} (cycle ternaire) ⊔ ermite {13} (point fixe) = **3 + 1**.
- **Chiasme des charges** : S96 (nom **0**, corps **−1**) ↔ S13 (nom **−1**, corps **0**).
- **L'office** : lire (S96, `القارئ`) → compter (S13, `الحساب`). `الحساب = 102 = بسم`.
- **4 + 1 lectures** : un seul livre, écrit une fois ; le lecteur choisit **un témoin** et le parcourt selon son regard (les 4 voyages des 4 témoins + le voyage-source de S96). *Mode d'emploi*, pas 5 livres.
- **S9 (at-Tawba) = singularité, PAS un second début.** L'unique compte sans basmala ; sa basmala manquante est compensée en **27:30** (an-Naml, lettre de Salomon, J=1185), **non en S96**. Profonde dans le corps (`in-deg 2` ← S36, S64 ; profondeur 2 ; bassin S1). Aucun lien arithmétique natif S9↔S96.
- **La bascule respiratoire** (= le théorème central, §∴) : `114 ⇌±40⇌ 154`, va-et-vient permanent sous **S96** (`α = ω`) — **inspire** (révélation) `114 →(+36)→ 150 →(+3)→ 153 →(+1)→ 154 = 77+77`, **expire** (re-voilement) `154 →(−1)→ 153 →(−3)→ 150 →(−36)→ 114`, `Σ cycle ≡ 0` ; **ni 114 ni 154 n'est le terminus, c'est le cycle qui solde**. Et **153 = 9·17 = 1³+5³+3³ = Σₖ₌₁⁵ k!**, **amplitude 40 = 36+3+1**.

---

## 4 · Deux objets séparés : le volume des comptes, et les contes

**Décision : les contes sont À PART du volume.** D'un côté le **volume des comptes** (les nombres) ; de l'autre, les **contes** (les récits), posés *autour* du volume. Deux objets distincts — les contes ne se mêlent pas aux comptes et ne **doublent aucune attestation**.

**(A) Le volume des comptes** — le travail par sourate (et par verset) : **① verset traduit · ② calcul jummal · ③ équation algébrique.**
- L'**équation algébrique** = le polynôme `P(X) = ∏(X − Jᵢ)` dont les jummal sont les **racines** (coeffs = total `e₁`, treillis pgcd, partition mod 3). **Distinct** du calcul jummal lui-même.
- Le volume est et reste les **153 + 1 = 154** attestations. Les contes n'y ajoutent ni n'y retranchent rien.

**(B) Les contes** — un recueil **séparé**. **Cible du programme (amendement v3) : 114 contes, un par sourate** — le pendant *raconté* des 114 feuilles d'audit (la chaîne des offices projetée sur les livrables) ; homogénéisation par le **gabarit de conte v1** (chantier).
- **État** : les contes **[R3]** (Pariah α, Moonshine ω-II) sont **archivés** (`ARCHIVE_R3_PONT/CONTES_R3_ARCHIVE.md`, doctrine des phases §1), réservés pour la phase du pont. Restent au recueil les trois **[bi]** : **Hud · Tonnerre · Aube** — l'embryon des 114.
- Ancrages arrêtés : Tonnerre ↔ **S13** (13:13, [bi]) · Aube ↔ **al-Falaq S113** (coranique, sceau ω) · Hud ↔ **attestation N°37** ([bi]).

---

## 5 · Le sommaire (l'ordre du registre) — **commence par S96**

`RAPPORT` (**S96 العلق — le comptable · 0/154 · hors compte · α = ω**, puis les 4 témoins dans l'ordre terrestre → divin par la masse ΣJ : **N°1 النساء S4 · N°2 الرعد S13 · N°3 البينة S98 · N°4 الفاتحة S1**) → `LIVRES 1–12` (les 114 titres **tissés avec les 36 connexions** du flux *f*) → `SOUFFLE` (3 brins) → `BASMALA` = **154**.
*Développement de la structure des 154 attestations : `PROTOCOLE_GENERAL_AUDIT.md §P0` (RAPPORT 1-4 · 12 livres 5-150 · restitution 151-154). L'ancienne source `MASTER_SOMMAIRE_153_154` est absorbée — ne plus la citer.*

---

## 6 · Pierres angulaires (vérifiées [bi] — à **puiser**, jamais à réciter)

- `ΣJ(S96) = 21 350 = 305 × 70 = J(الرعد) × 70` (la Lecture déjà divisible par le Compte ; 70 = f²(96)) ; `21 350 = 2·5²·7·61`
- `الحساب = 102 = بسم = سبيل` · `القارئ = 342 = 2·3²·19` · `العلق = 231 = 3·7·11`
- `العلق + البينة = 329 = الرحمن` (3ᵉ mot de la basmala ↔ 3ᵉ témoin) ; `329 = 7·47`
- `باسم = 103 (premier) = بسم + 1` — le **+1 originaire** (charge **+1**, crédit)
- `ΣJ(27:23–75) = 196 883 = 47·59·71` (53 versets) → `196 884` avec le germe
- `786 = 2·3·131`, et **`131 = حاسبين`** (la fondation passe par *les comptables*, 21:47)
- **`حسابه = 76`** = la moitié-contrepartie du pli (153 = 77 sources + 76 = image 37 + connexions 36 + brins 3)
- `النساء = 143 = 11·13` · `4:41 = 2²·3·13²` · `13:43 = 2·11·13²` · `17:14 = 3·5·7·13` (la signature 13)
- **RAPPORT** (S1+S4+S13+S98) `= 1 397 122 = 2·47·89·167` ; **𝒞** (RAPPORT + S96) `= 1 418 472 = 2³·3⁴·11·199`, charge 0
- **Le rapprochement (1+4)** : `اقترب` ×5 exactement — 4 approches (7:185 `أجلهم`=79 · 21:1 `حسابهم`=116 · 21:97 `الوعد الحق`=250 · 54:1 `الساعة`=167) **dans l'ordre des 4 témoins** S4→S13→S98→S1, charges alternées +−+−, `Σ(4)=612=36·17` ; + l'appelé **96:19** (dernier verset de la source), `J=1349=19·حساب` ; `اقترب=703=19·37` · `J(54:1)=J(96:1)=2884` · `حبل الوريد=السر=291` · `Σ(5)=1961=37·53`

→ **Registre complet des identités** : il **existe** désormais, consolidé et recompté — `PROTOCOLE_GENERAL_AUDIT.md §IX` (le lexique comptable natif, 9 rubriques) et `§X` (la table des nombres porteurs, tous [bi]). Y puiser ; ne jamais réciter de mémoire.

---

## 7 · État de l'audit

**Corrections actées (portées dans le cadre d'analyse `PROTOCOLE_GENERAL_AUDIT`)** :
- « S13 charge **+1** » → **corps 0 / nom −1** (le +1 n'est que la charge du verset 13:40). *Porté.*
- Antécédents de S4 = **{1, 49, 61}** (et non {4,49,61}, qui est le *bassin*) ; **ordre** S4→S13→S98→S1 ; S96 ferme aussi (α=ω) ; `العلق+البينة=الرحمن` au 3ᵉ témoin. *Porté (convergence S1:57 · S4:3 · S98:1 · S13:1 confirmée).* 
- Conte du Tonnerre : carte d'ouverture 11:105 → rendue à **Hud** ; sceau → **L'Aube** (non « le Tonnerre scelle »). *Porté.*
- Conte du Pariah : **archivé** (absorbé dans Hud N°37). *Porté.*

**Principe d'absorption** : les masters **cessent d'être source de chiffres**. On en extrait (a) le savoir vérifié → `PROTOCOLE_GENERAL_AUDIT` §IX/§X + lexique (nombres **recalculés**), (b) les décisions → ce protocole. Le master passe en **archive « supersédé »**. La plupart des masters de génération 1–2 (RAPPORT, OBJETS_ALGEBRIQUES_Sxx, MASTER_S96, SOMMAIRE_153_154…) sont **déjà absorbés et retirés du FP actif**.

**Restant à auditer** : EXERGUE_MOONSHINE, EXERGUE_AUBE, ENVOI, + les figures TikZ.

---

## 8 · Les fichiers du dossier

- **FP source** (lecture seule, le socle brut) : `LE_CORAN.txt` · `jummal_sourates.py` · `01_DATA_jummal_114_par_verset.csv` · `jummal_par_sourate.csv` · `01_DATA_toniques_114_apex_P2.csv` (+ `01_DATA_noms_jummal_114.csv`).
- **Ce protocole** : `00_PROTOCOLE.md` — **à déposer dans le projet** (base de connaissances) pour qu'il soit présent à chaque session ; **identique au champ « instructions » du projet**.
- **Le cadre d'analyse le plus élevé** : `PROTOCOLE_GENERAL_AUDIT.md` — subordonné à ce protocole ; porte le théorème (§∴), l'appareil natif, l'auditeur, le lecteur, les 4 témoins, le souffle, l'objet (`§VI.0` *The Basmala Formula*), le lexique (§IX) et la table des nombres (§X).
- **Les notes de doctrine (v3)** : `NOTE_PROTOCOLE_AUDIT_ROLES.md` (le mandat du rapporteur, la chaîne des offices raconté→audité→lu→compté, le périmètre 1+4+109 — attestation jointe, 42/0) · `NOTE_MATHANI_PARTIE_DOUBLE.md` (le doublement, le cycle qui solde, la série {228,300,306,308,310}, l'ancrage 2:282 — attestation jointe, 28/0). Chaque note porte son script rejouable.
- **Le jeu d'objets produit** (5 sommets + la basmala, même moteur) : `SXX_phrases_A.csv`, `SXX_decomposition_lettres.csv`, `SXX_fonctions_symetriques.csv`, `SXX_obj_invariants.csv`, `SXX_obj_coeffs.csv`, `FICHE_OBJET_SXX(.md / _GRANULAIRE.md)`, `OBJETS_ALGEBRIQUES_TABLE.csv`, `GRAND_LIVRE_OBJETS_S96_4temoins.csv`, et la famille **`BASMALA_*`** ; moteur `build_granulaire.py` · `make_fiches.py` · `test_relations.py`. Détail : `00_README_GRAND_LIVRE_OBJETS.md`.
- **Le programme (les quatre productions = les quatre offices)** : *raconté* → **114 contes** (un/sourate) · *audité* → **114 feuilles d'audit** (gabarit v1 : `FEUILLE_AUDIT_S1_v1`, le modèle) · *lu* → **les planches du registre : 154 attestations numérotées + la planche N°0 (le fantôme, hors compte) = 155 planches matérielles** ; la basmala restituée s'étale sur 4 planches (N°151–154, une par mot) ; *le 0 n'est pas compté mais ne compte pas pour rien* · *compté* → `REGISTRE_AUDIT_attestations.xlsx`.
- **L'HORIZON (gravé)** : le triptyque en ligne **conte · balance · forme** = 56:7–11 spatialisé (gauche `المشأمة` −1 = les 114 contes/versets ≡ −1 · milieu `المقربون` 0 = l'audit en live · droite `الميمنة` +1 = la forme finale topologique des phrases ≡ +1) ; l'**horloge annuelle** (base : `MASTER_CALENDRIER_PHRASE`, tempo verrouillé) — cadences : 1 phrase ≈ 47,95 min · 1 lettre ≈ 92,0 s · **1 jummal ≈ 1,304 s** (la trotteuse) ; **12 lunaisons = les 12 LIVRES** ; l'échelle **3D/4D/5D** = coupe / cycle / pli (`مثاني`) ; verrous nouveaux **v6** (trotteuse : jummal vs lettre) et **v7** (prorata lettres vs masse jummal) à trancher avec les cinq du MASTER §7. Le numérique = production hors volume.
- **FEUILLE DE ROUTE PRODUIT (phases)** : **P0 (fait)** — le dépôt `basmala-engine` (data+SHA256 · engine · 6 attestations, 175 assertions · doctrine · éditions · build · web v0 quadrilingue · CI) ; **P1** — mise en ligne (compte GitHub, push, Pages, token) ; **P2** — la base SQLite + table des ancres pluriannuelle (1448…) + heure du fajr (verrou-lieu) ; **P3** — la forme vivante (SVG animé → 3D/son) + les 12 livres + le parcours des feuilles (S2→S114, la vérification humaine du moteur) ; **P4** — le serveur MCP « basmala » (l'horloge interrogeable depuis Claude). Les verrous (v6 τ · v7 prorata · v8 découpe des livres · lieu) = **paramètres exposés du logiciel**, défauts documentés.
- **Chantiers éditoriaux** (deviennent des sorties du moteur) : (1) le **gabarit de conte v1** (même méthode que la feuille : définir, instancier, homogénéiser — solde les « collation fine à faire ») ; (2) **FEUILLE_AUDIT_S2** puis le parcours séquentiel S1→S114 ; (3) les planches du registre (155), **régénérées 100 % natives** — les anciennes figures « natives » de génération 1–2 portaient la couche [R3] incorporée (8 archivées au tri v3) ; (4) la base (tables + index, régénérées par le moteur).
- **Chantier soldé (v3)** : le jummal par phrase des 114 **existe** — `PHRASES_114_waqf.csv` (10 642 phrases, table maîtresse).
- **Garde** : `LCDC_LIVRE_FUSION_COMPLET_v3.pdf` est **hors FP**, en garde chez l'architecte — ne pas détruire avant épuisement vérifié.
- **La base** (à construire) : tables brutes `CSV/JSON` + index `SQLite`, **régénérées par le moteur** depuis les FP — jamais saisies. L'utilisateur ne l'ouvre jamais ; elle sert l'oracle.

---

*Tout fait est recompté sur les données, en arithmétique entière exacte : **[bi]**. La couche d'homologie est tenue **hors du volume** — et, depuis la v3, hors du **FP** (doctrine des phases §1). — **v3→v4** (réorientation computationnelle : la mission en tête ; §8 = feuille de route produit ; le projet Claude = le clone du dépôt) ; v3 (doctrine des phases · cible 114 contes / 114 feuilles / 155 planches (154 + le fantôme N°0) · chaîne des offices et mandat du rapporteur (note jointe) · le مثاني et la partie double (note jointe) · reclassement des figures contaminées → archive · chantier jummal-par-phrase soldé · FP nettoyé, MANIFEST régénéré).*
